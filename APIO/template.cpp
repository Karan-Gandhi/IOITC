#include <bits/stdc++.h>
using namespace std;

#define all(v) v.begin(), v.end()
#define pl(var) " [" << #var << ": " << (var) << "] "
#define endl '\n'
#define ll long long

int main() {
	ios::sync_with_stdio(false);
	cin.tie(nullptr);
}